apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata:
  namespace: tekton
  name: trigger-binding
spec:
  params:
  - name: revision
    value: ""
  - name: repo-url
    value: $(body.repository.url)